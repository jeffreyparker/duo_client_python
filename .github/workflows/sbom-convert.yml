name: Workflow for converting CycloneDX SBOM files to SPDX format

on:
  workflow_call:

jobs:
  convert:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@master
        with:
          name: cyclonedx-json
          path: ./cyclonedx-sbom.json
      - name: Convert SBOM
        run: |
          wget https://github.com/CycloneDX/cyclonedx-cli/releases/download/v0.24.2/cyclonedx-linux-x64
          chmod u+x cyclonedx-linux-x64
          ls -l ./cyclonedx-sbom.json
          touch ./cyclonedx-sbom.json
          ./cyclonedx-linux-x64 convert --input-format json --output-format spdxjson --input-file cyclonedx-sbom.json --output-file spdx.json
      - uses: actions/upload-artifact@master
        with:
          name: spdx-sbom
          path: spdx.json
